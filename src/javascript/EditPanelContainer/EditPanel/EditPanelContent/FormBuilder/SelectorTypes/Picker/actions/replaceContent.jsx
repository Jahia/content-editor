export const replaceContent = {
    init: context => {
        // Todo
        console.log(context);
    },
    onClick: context => {
        context.open(true);
    }
    // Todo finalize the replace content action
};
